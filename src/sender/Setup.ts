export const setup = (args: Array<string>): string => {
    switch (args[0]) {
        case '채널':
        case '클랜주소':
        default:
    }
    return 'will be support';
};

export const changePrefix = (newPrefix: string): void => {
    return;
};
